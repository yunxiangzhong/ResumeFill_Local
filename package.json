{
  "name": "resumefill-local",
  "version": "0.2.1",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:combobox": "playwright test tests/combobox.spec.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
